{
  "path": ["git", "remote-sync-check"],
  "kind": "command",
  "title": "Remote sync check",
  "description": "Check local branches against their configured remotes",
  "order": 20,
  "usage": "roborepo git remote-sync-check [path] [--fetch] [--json|--markdown|--table|--compact] [--include-clean] [--max-depth <n>]",
  "arguments": [{ "name": "path", "required": true, "interactive": { "prompt": "Folder to scan" } }],
  "interactiveArgs": ["--compact"],
  "onComplete": "returnToSubmenu",
  "continuePrompt": "Press Enter to return to git",
  "continueKey": "enter",
  "execution": {
    "adapter": "module",
    "module": "scripts/maintenance/git-inventory.mjs",
    "export": "remoteSyncCheck"
  }
}
