# Prompt Contracts

Portal-generated prompts are starting points, not evidence.

Repository-aware prompts name repository-relative plan paths and instruct the agent to verify claims against the current repository.

Portable prompts include repository name, relative path, metadata, warnings, and a bounded excerpt. They must say when context is omitted and ask for more repository context when needed.
