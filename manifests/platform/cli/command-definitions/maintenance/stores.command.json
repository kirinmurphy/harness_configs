{
  "path": ["maintenance", "stores"],
  "kind": "command",
  "title": "Local stores",
  "description": "Show what roborepo keeps on disk and how close each store is to its bound",
  "order": 40,
  "usage": "roborepo maintenance stores [list | reset <id> [--all]]",
  "onComplete": "returnToSubmenu",
  "continuePrompt": "Press any key to return to maintenance",
  "execution": {
    "adapter": "module",
    "module": "scripts/cli/maintenance-stores.mjs",
    "export": "maintenanceStores"
  }
}
