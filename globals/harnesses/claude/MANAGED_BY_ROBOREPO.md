# Managed By Roborepo

Stable Claude config is version-controlled in:

this repository.

Prefer editing the repo copy, not ad hoc files in `~/.claude`.

If a Claude config path is added or changed directly under `~/.claude`, update this repo so the change is version-controlled. Use `./scripts/sync-from-home.sh` only for intentionally capturing selected live config back into the repo.
