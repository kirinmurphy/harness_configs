{
  "path": ["package", "disable"],
  "kind": "command",
  "title": "Disable package",
  "description": "Disable a package and remove owned contributions live",
  "order": 50,
  "usage": "roborepo package disable <package-id> [--dry-run] [--cascade]",
  "arguments": [{ "name": "package-id", "required": true, "interactive": { "prompt": "Package id" } }],
  "execution": {
    "adapter": "module",
    "module": "scripts/cli/packages.mjs",
    "export": "disablePackage"
  }
}
